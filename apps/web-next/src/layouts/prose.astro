---
import Layout from './layout.astro';

type Props = {
  frontmatter: { title: string };
};
---

<Layout padded title={Astro.props.frontmatter.title}>
  <div class="mx-auto max-w-3xl text-base leading-7 text-gray-700">
    <slot />
  </div>
</Layout>
