{
  "name": "@letschurch/util",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
1:     ".": "./src/index.ts",
    "./rate-limit": "./src/rate-limit.ts",
    "./request-ip": "./src/request-ip.ts"
2: export { readRequestBody, RequestBodyTooLargeError } from './read-request-body';
  },
  "scripts": {
    "check": "pnpm run /^check:/",
    "check:fmt": "oxfmt --check",
    "check:lint": "oxlint",
    "check:ts": "tsc --noEmit",
    "fix": "oxfmt && oxlint --fix",
    "fix!": "oxfmt && oxlint --fix-dangerously"
  },
  "dependencies": {
    "pino": "catalog:",
    "pino-pretty": "^13.1.2",
    "type-fest": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "typescript": "catalog:"
  }
}
