---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: background-low-priority-worker
  labels:
    app: background-low-priority-worker
spec:
  replicas: 0
  selector:
    matchLabels:
      app: background-low-priority-worker
  template:
    metadata:
      labels:
        app: background-low-priority-worker
    spec:
      restartPolicy: Always
      initContainers:
        - name: db-migrate
          image: registry.gitlab.com/letschurch/lets.church/background-worker:latest
          command: ["sh", "-c", "cd /usr/src/app/packages/db && pnpm run prisma:migrate:deploy"]
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: pg-pguser-letschurch
                  key: uri
        - name: elasticsearch-migrate
          image: registry.gitlab.com/letschurch/lets.church/background-worker:latest
          command: ["sh", "-c", "cd /usr/src/app/packages/elasticsearch && pnpm run push-mappings"]
          envFrom:
            - configMapRef:
                name: web-config
      containers:
        - image: registry.gitlab.com/letschurch/lets.church/background-worker:latest
          name: background-low-priority-worker
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: pg-pguser-letschurch
                  key: uri
            - name: SENTRY_DSN
              valueFrom:
                secretKeyRef:
                  name: sentry-secret
                  key: SENTRY_BACKGROUND_WORKER_DSN
            - name: SERVICE_NAME
              value: background-low-priority-worker
          envFrom:
            - configMapRef:
                name: web-config
            - configMapRef:
                name: temporal-client-config
            - configMapRef:
                name: background-worker-config
            - configMapRef:
                name: background-low-priority-worker-config
            - secretRef:
                name: axiom-pino-secret
            - secretRef:
                name: s3-secret
            - secretRef:
                name: smtp-secret
