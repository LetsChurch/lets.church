---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: transcode-worker
  labels:
    app: transcode-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transcode-worker
  template:
    metadata:
      labels:
        app: transcode-worker
    spec:
      restartPolicy: Always
      containers:
        - image: registry.gitlab.com/letschurch/lets.church/transcode-worker:latest
          name: process-upload-worker
          envFrom:
            - configMapRef:
                name: temporal-client-config
            - configMapRef:
                name: transcode-worker-config
            - secretRef:
                name: s3-secret
