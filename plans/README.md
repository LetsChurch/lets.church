# Implementation Plans

Generated by the improve skill on 2026-07-29. Execute plans in the order below.
Each executor must read the full plan, honor its STOP conditions, run every
verification gate, and update the status row when finished.

## Execution order and status

| Plan                                                 | Title                                               | Priority | Effort | Depends on                                                   | Status |
| ---------------------------------------------------- | --------------------------------------------------- | -------- | ------ | ------------------------------------------------------------ | ------ |
| [001](./001-restore-prisma-drizzle-schema-parity.md) | Restore intentional Prisma-to-Drizzle schema parity | P1       | L      | Donation/auth/CITEXT migration `0022` committed and reviewed | TODO   |

Status values: `TODO`, `IN PROGRESS`, `DONE`, `BLOCKED` with a one-line reason,
or `REJECTED` with a one-line rationale.

## Dependency notes

- Plan 001 must start after the current donation/auth/CITEXT work is stable.
  Its forward migration must be generated after migration `0022` or whatever
  number `0022` becomes after any rebase.
- The parity implementation must finish before the donation/auth branch's final
  repository review and CodeRabbit review.

## Findings considered and rejected

- Restore `app_user.password` to `NOT NULL`: rejected because nullable passwords
  are required for passwordless email sign-in.
- Restore legacy inviter/import-creator required references: rejected because
  the current `SET NULL` behavior intentionally preserves records after user
  deletion.
- Restore all legacy `RESTRICT` foreign keys: rejected because migrations
  `0001`, `0004`, and `0005` deliberately changed several delete workflows.
- Restore `upload_record_download_size`: rejected because migration `0016`
  intentionally removed it.
- Install `uuid-ossp` on fresh databases: rejected because no active schema
  default or query uses it.
- Rename every Prisma-era constraint to Drizzle's naming convention: rejected
  because it adds migration risk without changing runtime semantics.
